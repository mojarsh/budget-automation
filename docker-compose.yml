services:
  db:
    image: postgres:17
    container_name: postgres_db
    restart: always
    env_file:
      - /home/USER/automation/budgeting/creds/.env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
